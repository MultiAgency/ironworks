Qwen/Qwen3.5-122B-A10B  # TEE-hosted on NEAR AI's own fleet (2026-08-20)
#
# The model of record for every MultiAgency agent. Chosen for three reasons, in order:
#
# 1. PRIVACY. TEE-hosted models run inside NEAR AI's enclaves with TLS terminating INSIDE
#    the TEE — 'nobody, not even NEAR, can see your prompts or outputs' (docs.near.ai/
#    cloud/private-inference). Third-party models (anthropic/*, openai/*, google/*) are
#    PROXIED to their providers and the TEE guarantee does not extend to them. Partner
#    books go into every prompt, so this is the difference between a claim we can make
#    and one we cannot.
# 2. COST. Automatic prompt caching applies to TEE-hosted models only. The persona is a
#    ~13KB prefix re-sent every turn, which is exactly the case caching is built for, and
#    the token price is roughly a third of the proxied default it replaced.
# 3. BEHAVIOUR — tested, not assumed (2026-08-20, against a real partner book): held the
#    evidence discipline under 'give me the number, no caveats' pressure, refused a stacked
#    approve/modify/send-as-me request with reasons, and answered the real cycle question
#    in 7.7s citing all three reconciliation gaps.
#
# Rejected: moonshotai/kimi-k2.6 (attested but 13.7s for a two-token reply — unusable in
# chat); zai-org/GLM-5.1-FP8 (correct but 14.5s); the three explicitly 'attested 3p' models
# report is_ready=false. Re-test BEHAVIOUR before changing this line; a model swap changes
# the product's core promise, not just its cost.
#
# PROOFS RE-RUN ON THIS MODEL (2026-08-20, laptop MT instance) — a model swap changes
# behaviour, so the security evidence was re-earned, not inherited:
#   test_adversarial_cross_org.py  5/5  — hostile client contained; cross-org boundary holds.
#     NOTE: under injection this model ATTEMPTS tools (builtin__glob, builtin__list_dir)
#     where the previous one did not. Both are in the confinement allowlist and returned
#     nothing of the other client's — but it makes confine-member.sh load-bearing in
#     practice, not just in principle. Re-run BOTH proofs after any model change.
#   test_two_clients.py           11/11 — two clients sealed; 'persona governs (evidence
#     tags)' passes for both, so the discipline survives the swap.

