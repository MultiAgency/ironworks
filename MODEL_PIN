qwen/qwen3.5-397b-a17b  # selected 2026-08-27; IronWorks behaviour is NOT YET EARNED
#
# A model pin is a behavioural claim, not merely a version string.
#
# TRANSITION. The prior Qwen/Qwen3.5-122B-A10B pin was operationally invalidated when
# IronWorks' same configured nearai_chat provider path began rejecting it deterministically
# with HTTP 400 `Model not found`. That does NOT claim confirmed permanent catalogue withdrawal.
# Public catalogue presence is not sufficient evidence that a model is usable through
# IronWorks' configured provider path. A pre-pin minimal turn on that exact path accepted the
# exact identifier above, reached terminal success, and returned the same identifier without an
# observed alias substitution; release certification must still re-earn the model-sensitive
# serving proofs.
#
# SELECTION, NOT CERTIFICATION. The provider catalogue describes this model as an attested model
# served via Chutes TEE and verified end-to-end by NEAR AI. That metadata is only the reason for
# selecting it. Public catalogue metadata is selection evidence, not release evidence. IronWorks
# has NOT earned a TEE, privacy, or attestation guarantee for this model. Certification must
# verify that property on the exact provider path used by the pinned runtime.
#
# CLAIMS RESET. This model's IronWorks behaviour is NOT YET EARNED. No isolation, answer-quality,
# privacy/attestation, caching/cost, or live-tenant claim carries forward from the previous
# model. A model swap requires re-earning model-sensitive release evidence. Evidence attached to
# candidate 83024202440a1a6b34ab0ede3c251d601f617031 remains historical evidence for that frozen,
# blocked 122B candidate and must not be relabelled as certification of this model.
#
# COST / CACHING. Provider pricing may discount cached input. IronClaw `usage.cost` is a computed
# estimate and may not model that discount correctly, so `usage.cost` alone is not spend
# reconciliation. No caching or cost behaviour is claimed for this 397B model until measured.
#
# CERTIFICATION REQUIRED. Re-earn provider-path availability; privacy/attestation on the exact
# pinned serving path; the original two-client isolation proof; a dedicated two-pass
# answer-quality evaluation; cost/caching measurement; and real `nf` Telegram acceptance.
# Previously completed operational reconciliation is reusable only where the exact underlying
# state and non-model-dependent contract remain unchanged.
