PGPW=<REPLACE — mint fresh on the host, do not reuse the spike dev value>
MASTER_KEY=<REPLACE — mint fresh on the host, do not reuse the spike dev value>
WEBUI_TOKEN=<REPLACE — mint fresh on the host, do not reuse the spike dev value>
WEBUI_USER=<REPLACE — mint fresh on the host, do not reuse the spike dev value>
NEARAI_API_KEY=<REPLACE — mint fresh on the host, do not reuse the spike dev value>
