# Proof fixtures

`proof-a` and `proof-b` are invented organizations for isolation tests. Their `.env.template`
files contain placeholders, guidance is slug-bound, and account domains use reserved `.example`
names. Copy them only into a private disposable test registry and never point destructive proofs
at real tenant identities.
