# proof-b — SYNTHETIC proof client (Harbor Studio Services). Template: copy to a
# private working dir as proof-b.env (chmod 600) and fill the two REQUIRED tokens from
# your own provisioning (multi/provision/provision.sh) — never commit a filled copy
# (the repo's *.env gitignore also refuses it). The group id is the repo's synthetic
# test id, not a real chat.
CLIENT_SLUG=proof-b
CLIENT_NAME="Proof Client B"
ORG_ID=proof-b
ACCOUNT_TOKEN=<REQUIRED — your Account-Service org token from provisioning>
IRONCLAW_TOKEN=<REQUIRED — your sealed IronClaw member token from provisioning>
IRONCLAW_USER_ID=<optional metadata>
TELEGRAM_GROUP_ID="-100900012"
